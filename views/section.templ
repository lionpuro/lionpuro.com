package views

templ section(id string) {
	<section id={ id } class="flex flex-col p-6 sm:p-8 gap-8">
		{ children... }
	</section>
}

templ sectionHeading() {
	<h2 class="text-2xl sm:text-3xl font-bold">
		{ children... }
	</h2>
}
